# ⚠️ SitRep auto-update needs review

Extracted health-zone counts did not validate against the total confirmed cases.

The update was stopped to avoid publishing incorrect values. Review extracted/sitrep_N031_review.json and, if present, extracted/openai_fallback_error.txt or extracted/openai_fallback_raw.json. OPENAI_API_KEY configured in runner: False; OpenAI fallback used: False.
